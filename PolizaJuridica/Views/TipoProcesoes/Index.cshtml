@model IEnumerable<PolizaJuridica.Data.TipoProceso>

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Orden)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Orden)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoProcesoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipoProcesoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoProcesoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
