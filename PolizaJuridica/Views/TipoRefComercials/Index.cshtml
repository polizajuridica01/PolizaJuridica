@model IEnumerable<PolizaJuridica.Data.TipoRefComercial>


<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoRepresentaRc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoDetalleRc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoRepresentaRc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoDetalleRc)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoRefComercialId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipoRefComercialId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoRefComercialId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
