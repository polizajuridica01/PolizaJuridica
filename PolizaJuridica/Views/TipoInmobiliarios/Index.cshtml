@model IEnumerable<PolizaJuridica.Data.TipoInmobiliario>

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoInmobiliarioDesc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clausula)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoInmobiliarioDesc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Clausula)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoInmobiliarioId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipoInmobiliarioId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoInmobiliarioId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
